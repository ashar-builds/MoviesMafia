@* Presentational poster card. On desktop the Trailer/Watch overlay reveals on
   hover; on touch devices (no hover) it stays visible so the actions are reachable. *@

<div class="group card relative overflow-hidden">
    @* Stretched link: makes the whole card navigate to details. It sits above the
       poster (z-10) but below the action controls (z-20), which re-enable their own
       pointer events so Trailer/Watch/info keep working. No <a> nesting — this is a
       sibling overlay, not an ancestor. *@
    @if (!string.IsNullOrWhiteSpace(DetailsUrl))
    {
        <a href="@DetailsUrl"
           class="absolute inset-0 z-10 rounded-card focus:outline-none focus-visible:ring-2 focus-visible:ring-brand/60"
           aria-label="View details for @Title"></a>
    }

    <div class="relative aspect-[2/3] overflow-hidden bg-surface-2">
        @if (PosterUrl is not null)
        {
            <img src="@PosterUrl" alt="@Title poster" loading="lazy"
                 class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105" />
        }
        else
        {
            <div class="flex h-full items-center justify-center text-muted">No image</div>
        }

        <div class="absolute right-2 top-2">
            <RatingBadge Score="Score" />
        </div>

        @if (!string.IsNullOrWhiteSpace(Badge))
        {
            <span class="chip absolute left-2 top-2 bg-ink/70 backdrop-blur">@Badge</span>
        }

        @* Overlay: gradient + overview + action buttons. Visible by default (touch),
           hover-reveal from sm: up where a pointer exists.
           The container is pointer-events-none so its gradient/overview never blocks
           the stretched details link — only the button row below re-enables clicks. *@
        <div class="pointer-events-none absolute inset-0 z-20 flex flex-col justify-end gap-3 bg-gradient-to-t from-ink via-ink/80 to-transparent p-3 opacity-100 transition-opacity duration-200 sm:opacity-0 sm:group-hover:opacity-100">
            @if (!string.IsNullOrWhiteSpace(Overview))
            {
                <p class="hidden line-clamp-4 text-xs text-content/90 sm:block">@Overview</p>
            }
            @* Only this row captures clicks (the rest of the poster falls through to the
               details link). On touch it's always interactive; from sm: up only on hover,
               so an invisible strip doesn't swallow clicks meant for details.
               whitespace-nowrap + min-w-0 prevents the play glyph from wrapping below
               the label on narrow cards (e.g. the 160px home-row cards). *@
            <div class="pointer-events-auto flex gap-2 sm:pointer-events-none sm:group-hover:pointer-events-auto">
                <a href="@TrailerUrl" class="btn-ghost min-w-0 flex-1 whitespace-nowrap px-2 py-2 text-xs sm:py-1.5">Trailer</a>
                <a href="@WatchUrl" class="btn-primary min-w-0 flex-1 whitespace-nowrap px-2 py-2 text-xs sm:py-1.5">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 shrink-0" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                        <path d="M6 4.5v11a.5.5 0 0 0 .77.42l9-5.5a.5.5 0 0 0 0-.84l-9-5.5A.5.5 0 0 0 6 4.5z" />
                    </svg>
                    <span>Watch</span>
                </a>
            </div>
        </div>
    </div>

    @* line-clamp-2 with min-height keeps card heights uniform while letting longer
       titles wrap to a second line instead of being mid-word truncated. *@
    <div class="p-3">
        <h3 class="line-clamp-2 min-h-[2.5rem] text-sm font-semibold leading-tight" title="@Title">@Title</h3>
        <div class="mt-1 flex items-center justify-between gap-2">
            @if (!string.IsNullOrWhiteSpace(Year))
            {
                <p class="text-xs text-muted">@Year</p>
            }
            else
            {
                <span></span>
            }
            @if (!string.IsNullOrWhiteSpace(DetailsUrl))
            {
                <a href="@DetailsUrl"
                   class="relative z-20 inline-flex h-7 w-7 shrink-0 items-center justify-center rounded-full border border-line bg-surface-2/60 text-content/80 transition hover:border-brand hover:bg-brand/10 hover:text-brand"
                   title="More info about @Title"
                   aria-label="More info about @Title">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                        <path fill-rule="evenodd" d="M18 10A8 8 0 11 2 10a8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd" />
                    </svg>
                </a>
            }
        </div>
    </div>
</div>

@code {
    [Parameter, EditorRequired] public string Title { get; set; } = "";
    [Parameter] public string? PosterUrl { get; set; }
    [Parameter] public string? Overview { get; set; }
    [Parameter] public string? Year { get; set; }
    [Parameter] public double Score { get; set; }
    [Parameter] public string? Badge { get; set; }
    [Parameter, EditorRequired] public string TrailerUrl { get; set; } = "";
    [Parameter, EditorRequired] public string WatchUrl { get; set; } = "";
    [Parameter] public string? DetailsUrl { get; set; }
}
